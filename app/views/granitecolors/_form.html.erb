<%= form_for @granitecolor do |f| %>
 <% if @granitecolor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@granitecolor.errors.count, "error") %> prohibited this Granitecolor from being saved:</h2>

      <ul>
      <% @granitecolor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form-group" align="left">
    <%= f.label :name, "Enter the name of your Granitecolor" %>
    <%= f.text_field :name, class: "form-control", placeholder: "Enter your Granite Color" %>
</div>

<div class="form-group" align="left">
    <%= f.label :url, "Enter the URL of the Granitecolor sample" %>
    <%= f.text_field :url, class: "form-control", placeholder: "Enter the URL" %>
</div>

 <div class="form-group">
    <%= f.submit class: "btn btn-primary btn-lg" %>
 </div>

<% end %>
